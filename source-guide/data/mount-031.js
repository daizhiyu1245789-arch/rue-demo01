rueSourceGuide.data["mount"].rows.push(...[
  {
    "file": "node_modules/@rue-js/runtime-vapor/dist/js-runtime/app.js",
    "line": 57,
    "code": "mount(app, container, render) {",
    "section": "C · 包装入口 → 原始 mount → render(app) 回调",
    "title": "appController.mount(app, container, render)",
    "note": "app 和 container 原样传入；形参 render = renderRoot。这里的 render 只是回调参数名。",
    "kind": "调用",
    "originalStep": 5,
    "sectionStart": false,
    "definitionId": "fn-29"
  },
  {
    "file": "node_modules/@rue-js/runtime-vapor/dist/js-runtime/app.js",
    "line": 65,
    "code": "assertActive();\nstate.lastContainer = container;\nconst existing = getAppMount(state, container);\nif (existing?.status === 'failed') {\n    throw existing.error;\n}\nif (existing && existing.owner !== state) {\n    throw new Error('Rue container is already mounted by another app.');\n}",
    "section": "C · 包装入口 → 原始 mount → render(app) 回调",
    "title": "检查 runtime 与容器事务",
    "note": "检查旧事务失败状态与 owner。后续源码进入首次挂载的事务创建。",
    "kind": "顺序",
    "originalStep": 5,
    "sectionStart": false,
    "definitionId": "fn-29"
  },
  {
    "file": "node_modules/@rue-js/runtime-vapor/dist/js-runtime/app.js",
    "line": 74,
    "code": "const transaction = existing ?? {\n    container,\n    error: undefined,\n    owner: state,\n    status: 'mounting',\n};\ntransaction.error = undefined;\ntransaction.status = 'mounting';\ntrackAppMount(state, transaction);\nconst previousActiveMount = state.activeAppMount;\nstate.activeAppMount = transaction;\ntry {\n    plugins.flush();",
    "section": "C · 包装入口 → 原始 mount → render(app) 回调",
    "title": "记录 transaction 与 activeAppMount",
    "note": "transaction.status = \"mounting\"。保存之前的 activeAppMount，最后要在 finally 恢复。",
    "kind": "顺序",
    "originalStep": 5,
    "sectionStart": false,
    "definitionId": "fn-29"
  },
  {
    "file": "node_modules/@rue-js/runtime-vapor/dist/js-runtime/plugins.js",
    "line": 43,
    "code": "flush() {\n    const installing = pending;\n    pending = [];\n    for (const { plugin, options } of installing) {\n        const install = readInstall(plugin);\n        if (!install)\n            continue;\n        try {\n            Reflect.apply(install, plugin, [undefined, options]);\n        }\n        catch {\n            // Plugin installation is a best-effort deferred task.\n        }\n    }\n},\nuse(plugin, options) {",
    "section": "C · 包装入口 → 原始 mount → render(app) 回调",
    "title": "plugins.flush()：先安装待处理插件",
    "note": "逐个读取 plugin.install 并 Reflect.apply(install, plugin, [undefined, options])；插件内部调用依其实现而定，执行返回后才继续 render(app)。",
    "kind": "内部调用",
    "originalStep": 5,
    "sectionStart": false,
    "definitionId": "fn-30"
  },
  {
    "file": "node_modules/@rue-js/runtime-vapor/dist/js-runtime/app.js",
    "line": 87,
    "code": "const result = render(app);",
    "section": "C · 包装入口 → 原始 mount → render(app) 回调",
    "title": "回到 appController：render(app)",
    "note": "render = renderRoot。把 app 作为实参调用；下一行回到 create-rue-base.js 的 root 回调。",
    "kind": "执行回调",
    "originalStep": 5,
    "sectionStart": false,
    "definitionId": "fn-29"
  },
  {
    "file": "node_modules/@rue-js/runtime-vapor/dist/js-runtime/create-rue-base.js",
    "line": 132,
    "code": "const value = typeof root === 'function'\n    ? storeMountInput(state, createElementMountInput(state, (props) => {\n        try {\n            return Reflect.apply(root, undefined, [props]);",
    "section": "C · 包装入口 → 原始 mount → render(app) 回调",
    "title": "进入 renderRoot(root)：root 接收 app",
    "note": "root === app === RootApp。先创建 (props) => { … } 包装组件；此处还没有执行 RootApp。",
    "kind": "执行回调",
    "originalStep": 5,
    "sectionStart": false,
    "definitionId": "fn-28"
  },
  {
    "file": "node_modules/@rue-js/runtime-vapor/dist/js-runtime/mount-input.js",
    "line": 117,
    "code": "export const createElementMountInput = (state, typeTag, propsValue, childrenValue, options = {}) => {\n    const props = copyProps(propsValue);\n    if (Array.isArray(childrenValue)) {\n        props.children = childrenValue;\n    }\n    else if (childrenValue != null) {\n        props.children = [childrenValue];\n    }\n    const children = normalizeChildren(state, effectiveChildren(props, childrenValue));\n    let type;\n    if (typeof typeTag === 'function') {\n        type = {\n            kind: 'component',\n            component: typeTag,\n            updateMode: 'rerender',\n        };\n    }\n    else if (typeTag === 'fragment') {\n        type = { kind: 'fragment' };\n    }",
    "section": "C · 包装入口 → 原始 mount → render(app) 回调",
    "title": "createElementMountInput()：包装组件变成 component 输入",
    "note": "typeTag = 上一步的 props 箭头函数；propsValue = {}；strictComponentReturns = true。type.component 保存这个函数。",
    "kind": "调用",
    "originalStep": 5,
    "sectionStart": false,
    "definitionId": "fn-31"
  },
  {
    "file": "node_modules/@rue-js/runtime-vapor/dist/js-runtime/mount-input.js",
    "line": 76,
    "code": "const createInput = ({ type, props = {}, children = [], source, strictComponentReturns = false, mountEffectScopeId, }) => {\n    const normalizedProps = copyProps(props);\n    const metadata = extractMetadata(source, normalizedProps);\n    return {\n        type,\n        props: normalizedProps,\n        children,\n        key: metadata.key,\n        strictComponentReturns,\n        mountCleanupBucket: metadata.mountCleanupBucket,\n        mountEffectScopeId: mountEffectScopeId ?? metadata.mountEffectScopeId,\n        elHint: undefined,\n        ...(source ? { portable: copyPortableKeys(source) } : {}),\n    };\n};\nconst normalizeChildren = (state, value) => {",
    "section": "C · 包装入口 → 原始 mount → render(app) 回调",
    "title": "createInput()：建立标准 MountInput",
    "note": "复制 props、提取 key/scope/cleanup 等元数据，返回 { type, props, children, … }。此时 type.kind = \"component\"。",
    "kind": "调用",
    "originalStep": 5,
    "sectionStart": false,
    "definitionId": "fn-32"
  },
  {
    "file": "node_modules/@rue-js/runtime-vapor/dist/js-runtime/mount-input.js",
    "line": 158,
    "code": "export const storeMountInput = (state, input) => {\n    const id = state.nextMountInputId++;\n    state.mountInputs.set(id, input);\n    const handle = { [RUE_MOUNT_ID_KEY]: id };\n    if (input.key !== undefined) {\n        handle.key = input.key;\n    }\n    return handle;\n};\nconst mountHandleId = (value) => {",
    "section": "C · 包装入口 → 原始 mount → render(app) 回调",
    "title": "storeMountInput(state, input)：保存输入并返回 handle",
    "note": "内层 createElementMountInput 返回之后，才调用外层 storeMountInput。返回值 value 是带 mount id 的 handle。",
    "kind": "调用",
    "originalStep": 5,
    "sectionStart": false,
    "definitionId": "fn-33"
  },
  {
    "file": "node_modules/@rue-js/runtime-vapor/dist/js-runtime/create-rue-base.js",
    "line": 145,
    "code": "const input = recordInput('render', value, [container]);",
    "section": "C · 包装入口 → 原始 mount → render(app) 回调",
    "title": "回到 renderRoot：recordInput(\"render\", value, [container])",
    "note": "把刚得到的 handle 交给 recordInput，接下来解析回标准输入。",
    "kind": "返回",
    "originalStep": 5,
    "sectionStart": false,
    "definitionId": "fn-28"
  },
  {
    "file": "node_modules/@rue-js/runtime-vapor/dist/js-runtime/create-rue-base.js",
    "line": 88,
    "code": "const recordInput = (entry, value, args) => {\n    assertRuntimeActive(state);\n    const input = normalizeMountInput(state, value, entry);\n    state.pendingInputs.push({ entry, input, args });\n    kernel.recordRuntimeInput(entry, input, args);\n    return input;\n};",
    "section": "C · 包装入口 → 原始 mount → render(app) 回调",
    "title": "recordInput() → normalizeMountInput() → 记录输入",
    "note": "normalizeMountInput 识别 handle，从 state.mountInputs 取回标准输入；随后压入 pendingInputs，并调用 kernel.recordRuntimeInput，最后返回 input。",
    "kind": "调用",
    "originalStep": 5,
    "sectionStart": false,
    "definitionId": "fn-34"
  },
  {
    "file": "node_modules/@rue-js/runtime-vapor/dist/js-runtime/create-rue-base.js",
    "line": 146,
    "code": "runRenderEntry(() => renderContainer(state, mountController, input, container));",
    "section": "C · 包装入口 → 原始 mount → render(app) 回调",
    "title": "runRenderEntry(箭头函数)",
    "note": "再传入一个回调：() => renderContainer(state, mountController, input, container)。",
    "kind": "传回调",
    "originalStep": 5,
    "sectionStart": false,
    "definitionId": "fn-35"
  },
  {
    "file": "node_modules/@rue-js/runtime-vapor/dist/js-runtime/create-rue-base.js",
    "line": 72,
    "code": "const runRenderEntry = (render) => {\n    state.renderDepth += 1;\n    try {\n        return render();\n    }\n    finally {\n        state.renderDepth -= 1;\n        if (state.renderDepth === 0)\n            flushPendingComponentLifecycle(state);\n    }\n};\nconst registerLifecycle = (register) => callback => {",
    "section": "C · 包装入口 → 原始 mount → render(app) 回调",
    "title": "runRenderEntry()：renderDepth++ → render()",
    "note": "render = 刚传入的箭头函数。执行 render() 后才进入 renderContainer；finally 要等整个渲染回调返回后才执行。",
    "kind": "调用",
    "originalStep": 5,
    "sectionStart": false,
    "definitionId": "fn-36"
  },
  {
    "file": "node_modules/@rue-js/runtime-vapor/dist/js-runtime/render/container.js",
    "line": 31,
    "code": "export const renderContainer = (state, controller, input, container) => {",
    "section": "D · 根容器 → 组件调用 → RootApp",
    "title": "renderContainer(state, controller, input, container)",
    "note": "controller = mountController；input.kind 在 input.type.kind 上，此时是 component。",
    "kind": "调用",
    "originalStep": 6,
    "sectionStart": true,
    "definitionId": "fn-37"
  },
  {
    "file": "node_modules/@rue-js/runtime-vapor/dist/js-runtime/render/container.js",
    "line": 39,
    "code": "const host = createHost(state.adapter);\nif (!host)\n    return;\nif (!input) {\n    clearContainer(host, state, container);\n    return;\n}\nconst previous = state.containerMounts.get(container);\nif (previous) {\n    const mounted = controller.patchMountedInput(state, host, previous, input, container);\n    if (mounted)\n        commitMountedContainer(host, container, mounted);\n    if (mounted)\n        state.containerMounts.set(container, mounted);",
    "section": "D · 根容器 → 组件调用 → RootApp",
    "title": "createHost(state.adapter) 与 previous 检查",
    "note": "host 提供绑定到 DOM adapter 的操作。首次挂载 previous 不存在，因此跳过 patch 分支。",
    "kind": "条件",
    "originalStep": 6,
    "sectionStart": false,
    "definitionId": "fn-37"
  }
]);
