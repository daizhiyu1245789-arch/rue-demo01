rueSourceGuide.data["router"] = {
  "title": "路由：RouterLink 点击如何经过导航事务更新 RouterView",
  "rows": []
};
rueSourceGuide.data["router"].rows.push(...[
  {
    "title": "createRouter()",
    "file": "node_modules/@rue-js/router/src/index.ts",
    "line": 701,
    "code": "export const createRouter = (options: RouterOptions): Router => {\n  const compilePath = (path: string) => {\n    // 将 `/users/:id(\\\\d+)` 这类模式编译成完整正则，同时记录捕获组对应的参数名。\n    const keys: string[] = []\n    const reStr =\n      '^' +\n      path.replace(/\\/:([^/()]+)(?:\\(([^)]+)\\))?/g, (_m, name, pattern) => {\n        // 累积命名参数键",
    "kind": "时序",
    "note": "createRouter() 返回 router 对象；useApp().use(router) 先排队，appController.mount() → plugins.flush() 才执行 router.install() → attachRouter(router)。",
    "watch": "routeByName、branches、options.routes",
    "section": "应用创建与路由安装",
    "originalStep": 1,
    "sectionStart": true,
    "definitionId": "fn-147"
  },
  {
    "title": "use 排队，mount 才 flush",
    "file": "node_modules/@rue-js/runtime-vapor/dist/js-runtime/app.js",
    "line": 60,
    "code": "         * 调用链：runtime.mount() -> appController.mount() -> plugins.flush() -> render(app)。\n         * 观察：existing、transaction、transaction.status。\n         */\n        // oxlint-disable-next-line no-debugger -- Rue 源码学习用自动断点。\n        if (globalThis.__RUE_RENDER_DEBUG__?.take('05.app-controller')) debugger;\n        assertActive();\n        state.lastContainer = container;",
    "kind": "衔接代码",
    "note": "plugins.flush()",
    "section": "应用创建与路由安装",
    "originalStep": 1,
    "sectionStart": false,
    "definitionId": "fn-29"
  },
  {
    "title": "useApp.use(router) → appRue.use(router)",
    "file": "node_modules/@rue-js/runtime/src/hooks/useApp.ts",
    "line": 73,
    "code": "    use(plugin: any, ...options: any[]) {\n      // 透传到 Rue.use，支持多插件链式安装\n      runWithClientRuntime(appRue, () => {\n        appRue.use(plugin, ...options)\n      })\n      return this\n    },",
    "note": "插件先排队；安装发生在随后 appController.mount 调 plugins.flush 时。",
    "kind": "登记",
    "section": "应用创建与路由安装",
    "originalStep": 1,
    "sectionStart": false,
    "definitionId": "fn-18"
  },
  {
    "title": "plugins.flush → Reflect.apply(install, plugin, …)",
    "file": "node_modules/@rue-js/runtime-vapor/dist/js-runtime/plugins.js",
    "line": 43,
    "code": "        flush() {\n            const installing = pending;\n            pending = [];\n            for (const { plugin, options } of installing) {\n                const install = readInstall(plugin);\n                if (!install)\n                    continue;\n                try {\n                    Reflect.apply(install, plugin, [undefined, options]);\n                }\n                catch {\n                    // Plugin installation is a best-effort deferred task.\n                }\n            }",
    "note": "install 此时是 router.install；传入 [undefined, options]。",
    "kind": "执行回调",
    "section": "应用创建与路由安装",
    "originalStep": 1,
    "sectionStart": false,
    "definitionId": "fn-30"
  },
  {
    "title": "router.install() → attachRouter()",
    "file": "node_modules/@rue-js/router/src/index.ts",
    "line": 1600,
    "code": "    install: (_app: unknown, _options: unknown[]) => {\n      // 将当前 Router 记录到容器映射，并设为活动路由\n      attachRouter(router)\n    },\n  }\n\n  __routerResolvePathByInstance.set(router, (to: RouteLocationRaw) => resolveLocation(to).href)\n  __routerPersistKeysByInstance.set(router, persistKeys)",
    "kind": "事件",
    "note": "安装阶段至此结束；用户之后点击 RouterLink 时，RouterLinkImpl 的 click handler 通过 useRouter() 取得同一实例",
    "watch": "getCurrentContainer()、__routerByContainer、__activeRouter",
    "section": "应用创建与路由安装",
    "originalStep": 2,
    "sectionStart": false,
    "definitionId": "fn-148"
  },
  {
    "title": "RouterLink 初始化时取得 r",
    "file": "node_modules/@rue-js/router/src/index.ts",
    "line": 1924,
    "code": "  const r = useRouter()\n  let clearPrefetchTrigger = () => {}\n  let linkElement: Element | null = null\n  const runPrefetch = () => {\n    void r.prefetch(to).catch(() => {})\n  }\n  const setUserRef = (value: Element | null) => {",
    "kind": "衔接代码",
    "note": "const r = useRouter()",
    "section": "应用创建与路由安装",
    "originalStep": 2,
    "sectionStart": false,
    "definitionId": "fn-149"
  },
  {
    "title": "RouterLinkImpl.click()",
    "file": "node_modules/@rue-js/router/src/index.ts",
    "line": 1961,
    "code": "  const click = (e: MouseEvent) => {\n    // 组件路径下使用上下文 Router，避免多个应用共存时误用活动 Router。\n    if (\n      (e as any).defaultPrevented ||\n      e.button !== 0 ||\n      e.metaKey ||\n      e.ctrlKey ||\n      e.shiftKey ||",
    "kind": "调用",
    "note": "click() 从已捕获的 r 选出 replace ? r.replace : r.push，然后 nav(to)。公开方法 push(p) → navigate(p, 'push')，replace(p) → navigate(p, 'replace')，第二个参数是方法名字符串。",
    "watch": "event.defaultPrevented、to、replace、r",
    "section": "用户点击 RouterLink 后的导航",
    "originalStep": 3,
    "sectionStart": true,
    "definitionId": "fn-150"
  },
  {
    "title": "点击调用 nav(to)",
    "file": "node_modules/@rue-js/router/src/index.ts",
    "line": 1975,
    "code": "    void nav(to)\n  }\n\n  // children 归一化为数组后透传给 h，其他属性直接落到最终的 a 元素上。\n  return h(\n    'a',\n    {",
    "kind": "衔接代码",
    "note": "void nav(to)",
    "section": "用户点击 RouterLink 后的导航",
    "originalStep": 3,
    "sectionStart": false,
    "definitionId": "fn-150"
  },
  {
    "title": "实际调用处",
    "file": "node_modules/@rue-js/router/src/index.ts",
    "line": 1561,
    "code": "    push: (p: RouteLocationRaw) => navigate(p, 'push'),\n    replace: (p: RouteLocationRaw) => navigate(p, 'replace'),\n    prefetch: async (p: RouteLocationRaw) => {\n      const resolution = resolveNavigationSync(p, route.get())\n      if (resolution.kind === 'error') {\n        throw resolution.error\n      }",
    "kind": "衔接代码",
    "note": "navigate(p, 'push')",
    "section": "用户点击 RouterLink 后的导航",
    "originalStep": 3,
    "sectionStart": false,
    "definitionId": "fn-151"
  },
  {
    "title": "push / replace → navigate",
    "file": "node_modules/@rue-js/router/src/index.ts",
    "line": 211,
    "code": "  push: (p: RouteLocationRaw) => Promise<NavigationFailure | undefined>\n  /** 替换当前历史记录并导航到目标位置。 */\n  replace: (p: RouteLocationRaw) => Promise<NavigationFailure | undefined>\n  /** 解析目标位置并预加载命中的懒路由组件，不执行守卫、导航或历史写入。 */\n  prefetch: (p: RouteLocationRaw) => Promise<void>\n  /** 等待当前导航队列完成，SSR 中通常在 push(url) 后调用。 */\n  isReady: () => Promise<void>",
    "note": "RouterLink 的 nav 是从 r.push/r.replace 取出的函数；rawPath 接收 to。",
    "kind": "调用",
    "section": "用户点击 RouterLink 后的导航",
    "originalStep": 3,
    "sectionStart": false
  },
  {
    "title": "navigate()",
    "file": "node_modules/@rue-js/router/src/index.ts",
    "line": 1267,
    "code": "  const navigate = async (\n    rawPath: RouteLocationRaw,\n    method: 'push' | 'replace',\n  ): Promise<NavigationFailure | undefined> => {\n    // 编程式导航先解析和跑守卫，只有 allow 后才委托给 history 修改地址。\n    const from = route.get()\n    const requestId = ++navigationRequestId\n    const manageFocus = shouldManageNavigationFocus()",
    "kind": "分支",
    "note": "navigate() 先 match(resolveLocation(rawPath).path) 供 before-navigation 事件使用，再选择 resolveNavigationSync() 快路径或 await resolveNavigation(rawPath, from, requestId)。两者内部会重新 match，并处理 redirect 后的新目标。",
    "watch": "from、requestId、navigationRequestId",
    "section": "用户点击 RouterLink 后的导航",
    "originalStep": 4,
    "sectionStart": false,
    "definitionId": "fn-152"
  },
  {
    "title": "同步导航快路径",
    "file": "node_modules/@rue-js/router/src/index.ts",
    "line": 1283,
    "code": "      const syncResolution = resolveNavigationSync(rawPath, from)\n      const canCommitSync =\n        syncResolution.kind !== 'allow' ||\n        (!hasBeforeEnterGuards(syncResolution.route) &&\n          !hasPendingLazyRouteComponents(syncResolution.route))\n\n      if (canCommitSync) {",
    "kind": "衔接代码",
    "note": "const syncResolution = resolveNavigationSync",
    "section": "用户点击 RouterLink 后的导航",
    "originalStep": 4,
    "sectionStart": false,
    "definitionId": "fn-152"
  },
  {
    "title": "异步守卫路径",
    "file": "node_modules/@rue-js/router/src/index.ts",
    "line": 1320,
    "code": "    const resolution = await resolveNavigation(rawPath, from, requestId)\n\n    if (resolution.kind === 'error') {\n      finishNavigation(resolution.route, from, method, resolution.error)\n      throw resolution.error\n    }\n",
    "kind": "衔接代码",
    "note": "const resolution = await resolveNavigation(",
    "section": "用户点击 RouterLink 后的导航",
    "originalStep": 4,
    "sectionStart": false,
    "definitionId": "fn-152"
  },
  {
    "title": "match()",
    "file": "node_modules/@rue-js/router/src/index.ts",
    "line": 859,
    "code": "  const match = (path: string): Route => {\n    // 按声明顺序匹配分支，命中后提取 params，并合并 matched 链上的 meta。\n    const normalizedPath = normalizeRoutePath(path)\n\n    for (let i = 0; i < branches.length; i++) {\n      const branch = branches[i]\n      const r = branch.record\n      // 顺序匹配，命中即返回（后续规则不再检查）",
    "kind": "返回",
    "note": "match() 把 Route 返回它的调用者。resolveNavigationSync/resolveNavigation 使用匹配结果处理 redirect 和守卫；不是把一次 match 的 Route 直接传进 resolveNavigation，它的第一个参数是原始地址 rawPath。",
    "watch": "normalizedPath、m、params、matched、meta",
    "section": "用户点击 RouterLink 后的导航",
    "originalStep": 5,
    "sectionStart": false,
    "definitionId": "fn-153"
  },
  {
    "title": "resolveNavigation()",
    "file": "node_modules/@rue-js/router/src/index.ts",
    "line": 1031,
    "code": "  const resolveNavigation = (\n    rawPath: RouteLocationRaw,\n    from: Route,\n    requestId: number,\n    redirectDepth = 0,\n  ): Promise<NavigationResolution> => {\n    const execute = async (): Promise<NavigationResolution> => {\n      // 一次导航解析包括：位置归一化、路由匹配、redirect 解析和守卫执行。",
    "kind": "分支",
    "note": "守卫允许继续后，导航事务调用 loadRouteComponents(to) 解析 matched 记录里的懒加载页面",
    "watch": "targetRoute、decision、redirectDepth",
    "section": "用户点击 RouterLink 后的导航",
    "originalStep": 6,
    "sectionStart": false,
    "definitionId": "fn-154"
  }
]);
